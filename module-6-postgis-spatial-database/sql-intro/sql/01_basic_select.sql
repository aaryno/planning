-- ===================================================================
-- Query 1: Basic SELECT Statement (2 points)
-- ===================================================================
--
-- OBJECTIVE: Write your first SQL query using basic SELECT syntax
--
-- TASK: Select all columns from the CITIES table and limit results to 8 rows
--
-- INSTRUCTIONS:
-- 1. Look at the Learning Example in the README.md file
-- 2. The example shows how to select from weather_stations with LIMIT 5
-- 3. You need to adapt it to select from cities with LIMIT 8
-- 4. Replace the table name and row limit below
--
-- EXPECTED RESULT:
-- You should see 8 rows showing all columns from the cities table
-- (city_id, name, state_code, population, latitude, longitude, elevation_ft)
--
-- ===================================================================

-- TODO: Write your SQL query below
-- HINT: Use SELECT * to get all columns, FROM to specify the table,
--       and LIMIT to restrict the number of rows

SELECT *
FROM _____________
LIMIT _________;

-- ===================================================================
-- CHECKLIST - Your query should include:
-- ☐ SELECT * (to get all columns)
-- ☐ FROM cities (to specify the correct table)
-- ☐ LIMIT 8 (to get exactly 8 rows)
-- ☐ Semicolon at the end
-- ===================================================================
