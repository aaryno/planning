# GIST 604B - Rasterio Real Data Download Requirements
# ===================================================
# This file lists all Python packages needed to download and process
# real geospatial data for the rasterio learning modules.

# Core raster processing
rasterio>=1.3.0
rioxarray>=0.15.0

# Vector data processing and integration
geopandas>=0.14.0
shapely>=2.0.0
fiona>=1.9.0

# Data manipulation and analysis
numpy>=1.21.0
pandas>=2.0.0

# Data downloading and progress tracking
requests>=2.28.0
tqdm>=4.64.0

# Visualization and mapping
matplotlib>=3.5.0
contextily>=1.4.0

# Cloud Optimized GeoTIFF creation
rio-cogeo>=3.5.0

# Optional: STAC (SpatioTemporal Asset Catalog) support
pystac>=1.8.0
pystac-client>=0.7.0

# Optional: Advanced raster analysis
xarray>=2023.1.0
rasterio-tester>=0.8.0

# Optional: NASA/USGS API access (for advanced users)
# earthaccess>=0.7.0
# landsat-util>=0.15.0

# Development and testing (optional)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# jupyter>=1.0.0
# jupyterlab>=4.0.0

# Note: Some packages like earthaccess and landsat-util are commented out
# because they require additional authentication setup. The script will
# work with fallback synthetic data if real data downloads fail.
#
# To install all requirements:
# pip install -r requirements.txt
#
# Or with conda:
# conda install -c conda-forge $(cat requirements.txt | grep -v "^#" | grep -v "^$")
